import { Story, Preview } from '@storybook/addon-docs/blocks';

# LoginForm

With `MDX` we can define a story for `LoginForm` right in the middle of our
markdown documentation.

<Preview withToolbar>
  <Story id="login-form--default" />
</Preview>
